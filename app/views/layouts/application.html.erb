<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>Cntrl</title>
    <%= csrf_meta_tags %>
    <%= yield :head %>
  </head>
  <body>
    <%= render "flashes" -%>
    <%= yield %>
    <%= render "javascript" %>
    <%= render "css_overrides" %>
  </body>
</html>
