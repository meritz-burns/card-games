en:
  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  activemodel:
    errors:
      models:
        card_movement:
          attributes:
            source:
              not_in_discard: "is not in the discard pile"
              not_in_hand: "is not in any player's hand"
              not_installed: "is not installed on any player's board"
              not_in_deck: "is not in the deck"

  discards:
    show:
      move_button: "Move into your hand"
  games:
    index:
      start: "Start a new game"
      join: "Join a game by number"
    new:
      start_button: "Start"
    show:
      inspect_discard: "Inspect"
      move_button: "Move"
  joins:
    new:
      join_button: "Join"
